@model BTIGradedWork.Controllers.CourseSelect

@{
    ViewBag.Title = "CourseSelection";
}

<h2>CourseSelection</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>CourseList</h4>
        <hr />
        @Html.ValidationSummary(true)

        @*<div class="form-group">
            @foreach (var c in ViewBag.Courses)
            {
                 @Html.LabelFor(model => model.Code, new { @class = "control-label col-md-2" })
            <label style="control-label col-md-2"><input type="checkbox" name="Course" value="@c.Value"/><span>@c.Text</span></label><br />
                <div class="col-md-10">

                </div>
            }
</div>*@

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
